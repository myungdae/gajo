import{PlaceDiscoveryService}from'./place-discovery.service';
const pension={entityUri:'pension',canonicalLabelKo:'합천호 스마일펜션',alternateLabels:['스마일펜션'],entityType:'ACCOMMODATION',category:'LODGING',tags:['REST'],runtimeDataStatus:'VERIFIED',latitude:35.524485899856,longitude:128.01578179029};const lowful={entityUri:'urn:regional:hapcheon:lowful',canonicalLabelKo:'로우풀',alternateLabels:[],entityType:'CAFE',category:'CAFE',tags:['CAFE','REST','HAPCHEON_LAKE'],runtimeDataStatus:'VERIFIED',latitude:35.525488,longitude:128.018877,telephone:'0507-1333-2434',actions:{call:{phone:'0507-1333-2434'},navigate:{latitude:35.525488,longitude:128.018877}}};const moida={entityUri:'moida',canonicalLabelKo:'카페모이다',alternateLabels:[],entityType:'CAFE',category:'CAFE',tags:['CAFE'],runtimeDataStatus:'VERIFIED'};const restaurant={entityUri:'food',canonicalLabelKo:'북어마을',alternateLabels:[],entityType:'RESTAURANT',category:'FOOD',tags:['FOOD','HAPCHEON_LAKE'],runtimeDataStatus:'VERIFIED',actions:{call:{phone:'055'}}};
describe('PlaceDiscoveryService',()=>{const regional={effectiveDataset:jest.fn(async(region:string)=>({regionId:region,records:region==='hapcheon'?[pension,lowful,moida,restaurant]:[]}))};const service=new PlaceDiscoveryService(regional as any);it('ranks DB-only ACTIVE metadata without a name boost and preserves actions',async()=>{const result:any=await service.discover('hapcheon','CAFE','합천호 주변에서 전망 좋은 카페에 가고 싶어요.',{activityPreferences:['CAFE','HAPCHEON_LAKE']});expect(result.entities.map((x:any)=>x.entityId)).toEqual(['urn:regional:hapcheon:lowful','moida']);expect(result.entities[0].actions).toEqual(lowful.actions);expect(result.entities.every((x:any)=>x.entityType==='CAFE')).toBe(true)});it('derives Smile Pension proximity from verified coordinates',async()=>{const result:any=await service.discover('hapcheon','CAFE','스마일펜션 근처 카페 알려줘.',{activityPreferences:['CAFE']});expect(result.anchorEntityId).toBe('pension');expect(result.entities[0]).toMatchObject({entityId:'urn:regional:hapcheon:lowful',distanceMeters:301})});it('returns restaurants only and never leaks another region',async()=>{const food:any=await service.discover('hapcheon','FOOD','합천호 주변 저녁 먹을 곳 알려줘.',{activityPreferences:['FOOD']});expect(food.entities.map((x:any)=>x.entityId)).toEqual(['food']);expect((await service.discover('okcheon','CAFE','근처 카페',{})).entities).toEqual([])})});
