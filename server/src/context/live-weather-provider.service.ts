import { Injectable,Optional } from '@nestjs/common';import{ConfigService}from'@nestjs/config';import type{NormalizedWeatherObservation,OperationalWeatherState}from'./runtime-context.types';import{RegionConfigService}from'../region/region-config.service';
export const DEFAULT_GAJO_LOCATION={latitude:35.7423,longitude:127.9528,timezone:'Asia/Seoul'}as const;export const WEATHER_THRESHOLDS={lightRainMm:0.1,rainMm:2.5,heavyRainMm:15}as const;
export interface WeatherRequest{regionId?:string;latitude?:number;longitude?:number;accuracy?:number;gpsAllowed?:boolean}
@Injectable()export class LiveWeatherProviderService{private readonly lastKnown=new Map<string,NormalizedWeatherObservation>();constructor(private readonly config:ConfigService,@Optional()private readonly regions?:RegionConfigService){}
get location(){return this.locationFor({regionId:'gajo'})!}locationFor(request:WeatherRequest){const regionId=request.regionId||'gajo';if(request.gpsAllowed&&Number.isFinite(request.latitude)&&Number.isFinite(request.longitude)&&Number.isFinite(request.accuracy)&&Number(request.accuracy)<=1500)return{latitude:Number(request.latitude),longitude:Number(request.longitude),timezone:'Asia/Seoul',sourceId:`gps:${Number(request.latitude).toFixed(3)},${Number(request.longitude).toFixed(3)}`};const configured=this.regions?.get(regionId).weatherReference;if(configured)return configured;if(regionId==='gajo')return{latitude:Number(this.config.get('GAJO_LATITUDE')??DEFAULT_GAJO_LOCATION.latitude),longitude:Number(this.config.get('GAJO_LONGITUDE')??DEFAULT_GAJO_LOCATION.longitude),timezone:this.config.get<string>('GAJO_TIMEZONE')||DEFAULT_GAJO_LOCATION.timezone,sourceId:'gajo-weather-reference'};return undefined}
async getCurrent(request:WeatherRequest={}):Promise<NormalizedWeatherObservation>{const regionId=request.regionId||'gajo',location=this.locationFor(request);if(!location)return{observedAt:new Date().toISOString(),weather:'UNKNOWN',source:'UNAVAILABLE',status:'UNAVAILABLE',stale:false};const cacheKey=`${regionId}:${location.sourceId}:${location.latitude.toFixed(3)}:${location.longitude.toFixed(3)}`;const params=new URLSearchParams({latitude:String(location.latitude),longitude:String(location.longitude),timezone:location.timezone,current:'temperature_2m,precipitation,rain,weather_code,wind_speed_10m,is_day'}),controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),Number(this.config.get('OPEN_METEO_TIMEOUT_MS')||3500));try{const response=await fetch(`https://api.open-meteo.com/v1/forecast?${params}`,{signal:controller.signal});if(!response.ok)throw new Error(`weather provider ${response.status}`);const observation=this.normalize((await response.json()as any).current||{});this.lastKnown.set(cacheKey,observation);return observation}catch{const stale=this.lastKnown.get(cacheKey);return stale?{...stale,status:'STALE',stale:true}:{observedAt:new Date().toISOString(),weather:'UNKNOWN',source:'UNAVAILABLE',status:'UNAVAILABLE',stale:false}}finally{clearTimeout(timeout)}}
normalize(current:any):NormalizedWeatherObservation{const precipitation=this.number(current.precipitation),weatherCode=this.number(current.weather_code);return{observedAt:current.time||new Date().toISOString(),weather:this.normalizeState(weatherCode,precipitation),temperature:this.number(current.temperature_2m),precipitation,rain:this.number(current.rain),weatherCode,windSpeed:this.number(current.wind_speed_10m),isDay:current.is_day===undefined?undefined:Number(current.is_day)===1,source:'OPEN_METEO',status:'LIVE',stale:false}}
private normalizeState(code?:number,precipitation=0):OperationalWeatherState{if(code!==undefined&&[95,96,99].includes(code))return'THUNDERSTORM';if(code!==undefined&&((code>=71&&code<=77)||[85,86].includes(code)))return'SNOW';if(precipitation>=WEATHER_THRESHOLDS.heavyRainMm||code===82)return'HEAVY_RAIN';if(precipitation>=WEATHER_THRESHOLDS.rainMm||(code!==undefined&&[55,56,57,63,65,66,67,80,81].includes(code)))return'RAIN';if(precipitation>=WEATHER_THRESHOLDS.lightRainMm||(code!==undefined&&[51,53,61].includes(code)))return'LIGHT_RAIN';if(code===0)return'CLEAR';if(code!==undefined&&[1,2,3,45,48].includes(code))return'CLOUDY';return'UNKNOWN'}private number(value:unknown){const parsed=Number(value);return value===undefined||value===null||!Number.isFinite(parsed)?undefined:parsed}}
