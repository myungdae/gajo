import{lstat,readFile,realpath}from'node:fs/promises';import{resolve,sep}from'node:path';import mongoose from'mongoose';import{authorizeCoreOperation,runCoreAlignment}from'./receipt32-core-align-core.mjs';
const usage=`Receipt 32 core maintenance (dry-run by default)\nUsage: npm run receipt32:core-align -- --core-pre-image <restricted-pre-image.ejson> --core-manifest <restricted-manifest.json> --actor <opaque-id> --reason <reason> [--restore] [--apply --confirm <approval>]`;
const flags=new Set(['--restore','--apply']),valued=new Set(['--core-pre-image','--core-manifest','--actor','--reason','--confirm']);
function args(argv){const parsed={};for(let i=0;i<argv.length;i++){const item=argv[i];if(flags.has(item)){parsed[item]=true;continue}if(!valued.has(item))throw new Error(`Unknown argument: ${item}`);const next=argv[++i];if(!next||next.startsWith('--'))throw new Error(`Missing value for ${item}`);parsed[item]=next}return parsed}
async function main(){if(process.argv.length===2){process.stdout.write(`${usage}\n`);return}let input;try{input=args(process.argv.slice(2))}catch(error){process.stderr.write(`${error.message}\n${usage}\n`);process.exitCode=2;return}const operation=input['--restore']?'restore':'align',approval=operation==='restore'?process.env.RECEIPT32_CORE_RESTORE_APPROVED:process.env.RECEIPT32_CORE_ALIGN_APPROVED;let apply;try{apply=authorizeCoreOperation({operation,apply:Boolean(input['--apply']),confirm:input['--confirm'],approved:approval})}catch(error){process.stderr.write(`${error.message}\n`);process.exitCode=1;return}for(const name of['--core-pre-image','--core-manifest','--actor','--reason'])if(!input[name]){process.stderr.write(`Missing required argument: ${name}\n${usage}\n`);process.exitCode=2;return}try{const root=await realpath(resolve(process.cwd(),'.maintenance-private','receipt32')),load=async name=>{const actual=await realpath(resolve(input[name]));if(!actual.startsWith(`${root}${sep}`)||(await lstat(actual)).isFile()===false)throw new Error('PRIVATE_INPUT_NOT_READY');return JSON.parse(await readFile(actual,'utf8'))},preImage=mongoose.mongo.BSON.EJSON.parse(JSON.stringify(await load('--core-pre-image')),{relaxed:false}),manifest=await load('--core-manifest'),uri=process.env.MONGODB_URI;if(!uri)throw new Error('MONGODB_URI_MISSING');await mongoose.connect(uri,{autoIndex:false,autoCreate:false});const collection=mongoose.connection.db?.collection(manifest.collection);if(!collection)throw new Error('COLLECTION_UNAVAILABLE');const result=await runCoreAlignment({collection,preImage,manifest,actorId:input['--actor'],reason:input['--reason'],apply,operation});process.stdout.write(`${JSON.stringify(result,null,2)}\n`)}catch(error){const known={ENOENT:'Private maintenance inputs are not prepared.',PRIVATE_INPUT_NOT_READY:'Private maintenance inputs are not prepared.',MONGODB_URI_MISSING:'MongoDB connection is not configured.',COLLECTION_UNAVAILABLE:'Configured collection is unavailable.'};const message=known[error?.code]||known[error?.message]||String(error?.message||'Core maintenance failed').replace(/mongodb(?:\+srv)?:\/\/\S+/gi,'[REDACTED_MONGODB_URI]');process.stderr.write(`Receipt 32 core maintenance failed: ${message}\n`);process.exitCode=1}finally{await mongoose.disconnect()}}
await main();
