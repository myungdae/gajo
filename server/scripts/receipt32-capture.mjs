import{createHash}from'node:crypto';
const usage='Receipt 32 read-only capture (no database writes)\nUsage: npm run receipt32:capture -- --before\nRuns only in the approved Docker one-off runtime; writes fixed private artifacts with mode 0600.';
const argv=process.argv.slice(2);if(argv.length===0){process.stdout.write(`${usage}\n`);process.exit(0)}if(argv.length!==1||argv[0]!=='--before'){process.stderr.write(`Unknown or incomplete capture arguments\n${usage}\n`);process.exit(2)}
let mongoose;try{mongoose=(await import('mongoose')).default;const{captureBefore}=await import('./receipt32-capture-core.mjs'),{writePrivateArtifacts}=await import('./receipt32-capture-files.mjs'),uri=process.env.MONGODB_URI;if(!uri)throw new Error('MONGODB_URI_MISSING');await mongoose.connect(uri,{autoIndex:false,autoCreate:false});const db=mongoose.connection.db;if(!db)throw new Error('COLLECTION_UNAVAILABLE');const result=await captureBefore({coreCollection:db.collection('coredestinations'),regionalCollection:db.collection('regionaldatarecords')});const EJSON=mongoose.mongo.BSON.EJSON,artifacts={'core-pre-image.ejson':EJSON.stringify(result.documents.core,{relaxed:false}),'garden-pre-image.ejson':EJSON.stringify(result.documents.garden,{relaxed:false}),...Object.fromEntries(Object.entries(result.files).map(([k,v])=>[k,JSON.stringify(v,null,2)]))};await writePrivateArtifacts({cwd:process.cwd(),artifacts});const files=Object.entries(artifacts).map(([name,value])=>({name,sha256:createHash('sha256').update(value).digest('hex')}));process.stdout.write(`${JSON.stringify({...result.summary,files})}\n`)}catch(error){const known={EEXIST:'Private capture artifacts already exist; nothing was overwritten.',OUTPUT_EXISTS:'Private capture artifacts already exist; nothing was overwritten.',MONGODB_URI_MISSING:'MongoDB connection is not configured.',ERR_MODULE_NOT_FOUND:'Maintenance runtime dependencies are unavailable; use the approved Docker one-off command.'};process.stderr.write(`Receipt 32 capture failed: ${known[error?.code]||known[error?.message]||'capture validation failed'}\n`);process.exitCode=1}finally{await mongoose?.disconnect()}
