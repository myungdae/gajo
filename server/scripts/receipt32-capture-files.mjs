import{chmod,lstat,mkdir,open,stat}from'node:fs/promises';import{resolve}from'node:path';
export async function writePrivateArtifacts({cwd,artifacts}){const root=resolve(cwd,'.maintenance-private','receipt32');await mkdir(root,{recursive:true,mode:0o700});await chmod(root,0o700);if(!(await stat(root)).isDirectory())throw new Error('PRIVATE_OUTPUT_NOT_READY');for(const name of Object.keys(artifacts)){if(name!==name.split(/[\\/]/).at(-1))throw new Error('UNSAFE_FILENAME');try{await lstat(resolve(root,name));throw new Error('OUTPUT_EXISTS')}catch(error){if(error.code!=='ENOENT')throw error}}const handles=[];try{for(const name of Object.keys(artifacts))handles.push(await open(resolve(root,name),'wx',0o600));for(let i=0;i<handles.length;i++){await handles[i].writeFile(artifacts[Object.keys(artifacts)[i]],'utf8');await chmod(resolve(root,Object.keys(artifacts)[i]),0o600)}}finally{await Promise.all(handles.map(h=>h.close()))}return root}
