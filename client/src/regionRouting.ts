import type{RegionId}from'./regionConfig.ts';export function regionFromLocation(pathname:string,search:string):RegionId{const segment=pathname.split('/').filter(Boolean)[0];if(segment==='gajo'||segment==='okcheon'||segment==='muan'||segment==='gyeryong'||segment==='hapcheon'||segment==='daejeon-junggu')return segment;const query=new URLSearchParams(search).get('region');return query==='okcheon'||query==='muan'||query==='gyeryong'||query==='hapcheon'||query==='daejeon-junggu'?query:'gajo'}export function regionalPath(path:string,regionId:RegionId,explicitGajo=false){if(regionId!=='gajo')return`/${regionId}${path}`;if(explicitGajo)return`/gajo${path}`;return path}
