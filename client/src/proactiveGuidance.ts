export type WeatherEvidence={target:{latitude:number;longitude:number;label:string};kind:'OBSERVATION'|'FORECAST';dataTime:string;fetchedAt:string;expiresAt:string;source:string;temperature?:number;precipitation?:number;windSpeed?:number};
export type PlaceEvidence={label:string;characteristic:string;source:string};
export type ProactiveGuidance={fact?:string;context:string;recommendation:string;source:{weather?:string;place:string};observedAt?:string;forecastFor?:string;freshness:'FRESH'|'STALE'|'UNAVAILABLE';fallbackUsed:boolean;basisLabel?:string};
const prohibited=/이번 주|요즘|가을철에는/;
export function buildProactiveGuidance(place:PlaceEvidence,weather?:WeatherEvidence,now=new Date()):ProactiveGuidance{const fresh=weather&&new Date(weather.expiresAt).getTime()>=now.getTime()&&new Date(weather.dataTime).getTime()<=new Date(weather.expiresAt).getTime();if(!weather||!fresh)return{context:place.characteristic,recommendation:'출발 전 목적지의 현지 날씨를 확인하고 필요한 준비물을 챙기세요.',source:{place:place.source},freshness:weather?'STALE':'UNAVAILABLE',fallbackUsed:true};const at=new Date(weather.dataTime).toLocaleTimeString('ko-KR',{timeZone:'Asia/Seoul',hour:'2-digit',minute:'2-digit',hour12:false});const fact=weather.kind==='OBSERVATION'?`현재 ${weather.target.label} 주변은${weather.temperature!==undefined?` ${weather.temperature}°C이고`:''}${(weather.precipitation||0)>0?' 비가 관측되고 있습니다':(weather.windSpeed||0)>=9?' 바람이 강합니다':' 날씨가 관측되었습니다'}.`:`${at} 무렵 ${weather.target.label} 주변에는${(weather.precipitation||0)>0?' 비가':(weather.windSpeed||0)>=9?' 강한 바람이':' 날씨 변화가'} 예보되어 있습니다.`;const recommendation=(weather.precipitation||0)>0?'우비와 미끄럼에 강한 신발을 준비하세요.':(weather.windSpeed||0)>=9?'노출된 구간을 피하고 겉옷을 준비하세요.':weather.temperature!==undefined&&weather.temperature<=8?'보온 겉옷을 준비하세요.':'현장 상황을 확인하며 이동하세요.';const result={fact,context:place.characteristic,recommendation,source:{weather:weather.source,place:place.source},observedAt:weather.kind==='OBSERVATION'?weather.dataTime:undefined,forecastFor:weather.kind==='FORECAST'?weather.dataTime:undefined,freshness:'FRESH'as const,fallbackUsed:false,basisLabel:`${at} 기준`};if(prohibited.test(`${result.fact} ${result.recommendation}`))throw new Error('Broad seasonal language is not allowed');return result}
