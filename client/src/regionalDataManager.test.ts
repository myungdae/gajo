import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';
test('admin regional data manager exposes filters KPIs comparison and explicit actions',()=>{const source=readFileSync(new URL('./components/RegionalDataManager.tsx',import.meta.url),'utf8');for(const text of ['전체 지역','전체 상태','전체 유형','전체 검증','현재 운영 데이터','새 후보/변경 제안','승인','보류','수정 후 승인','거절','운영 중지','재검증 필요','변경 반영','변경 무시'])assert.match(source,new RegExp(text));for(const key of ['totalActive','awaitingVerification','needsReverification','changeDetected','missingCoordinates','missingPhone','missingActions'])assert.match(source,new RegExp(key))});
test('admin writes send a dedicated token while reads remain filterable',()=>{const source=readFileSync(new URL('./api/client.ts',import.meta.url),'utf8');assert.match(source,/fetchRegionalData/);assert.match(source,/x-admin-token/);assert.match(source,/regional-data\/candidates/);assert.match(source,/actions\/\$\{action\}/)});
test('admin review uses friendly provenance labels and exposes discovered semantic facts',()=>{const source=readFileSync(new URL('./components/RegionalDataManager.tsx',import.meta.url),'utf8');for(const text of ['한국관광공사','지자체 공식 정보','공식 사업자','공식 지도 정보','displayName','category','tags','shortDescription','근거 열기'])assert.match(source,new RegExp(text))});
test('regional transfer UI defaults to staged review and exposes explicit trusted import',()=>{const source=readFileSync(new URL('./components/RegionalDataManager.tsx',import.meta.url),'utf8');for(const text of ['데이터 관리','운영 데이터 내보내기','데이터 가져오기','가져오기 검토','검증 대기로 가져오기','trusted verified import','신규','충돌','변경 없음'])assert.match(source,new RegExp(text));assert.match(source,/useState\(false\)/);assert.match(source,/1_000_000/)});
test('regional transfer calls remain protected by the admin token',()=>{const source=readFileSync(new URL('./api/client.ts',import.meta.url),'utf8');for(const name of ['exportRegionalData','previewRegionalDataImport','importRegionalData'])assert.match(source,new RegExp(name));assert.match(source,/regional-data\/export/);assert.match(source,/regional-data\/import\/preview/);assert.match(source,/x-admin-token/g)});
