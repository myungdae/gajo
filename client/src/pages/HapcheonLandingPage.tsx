import { Link } from 'react-router-dom';
import { useRegion } from '../RegionContext';
import RegionalLandingShare from '../components/RegionalLandingShare';
// Region configuration supplies landing.ctaLabel; the fallback keeps new regions usable.
export default function HapcheonLandingPage(){const region=useRegion(),landing=region.landing;return <main className="regional-landing" style={{'--landing-background':`url(${landing?.backgroundImage})`,'--landing-accent':region.accent}as React.CSSProperties}><div className="regional-landing-shade"/><section className="regional-landing-content" aria-labelledby="regional-landing-title"><header><p>{region.regionName} 여행</p><h1 id="regional-landing-title">{landing?.title||region.heroTitle}</h1><p>{landing?.description||region.heroCopy}</p></header><RegionalLandingShare/><Link className="regional-landing-cta" to={`/${region.id}?start=ai`} onClick={()=>sessionStorage.setItem('hapcheon-landing-complete','1')}>{landing?.ctaLabel||`${region.regionName} 여행 시작하기`} <span aria-hidden="true">›</span></Link></section></main>}
