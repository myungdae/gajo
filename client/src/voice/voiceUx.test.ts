import test from "node:test";import assert from "node:assert/strict";import{acceptVoiceResult,understandVoice,updateVoiceSlot,voiceConfirmationPolicy,voiceExecutionText,voiceNeedsConfirmation,voiceStateMessage}from"./voiceUx.ts";
test("one utterance splits into independently confident slots",()=>{const r=understandVoice("미가추어탕 주변 식당 찾아줘","가조면 현재 위치");assert.equal(r.slots.place.value,"미가추어탕");assert.equal(r.slots.action.value,"주변 장소 찾기");assert.equal(r.slots.category.value,"음식점");assert.equal(r.slots.referenceLocation.value,"미가추어탕 주변");assert.deepEqual(voiceNeedsConfirmation(r),[])});
test("golden intents retain place and derive action",()=>{for(const[u,p,a]of[["황매산 지금 추워?","황매산","현재 날씨 확인"],["해인사 몇 시까지 해?","해인사","운영시간 확인"],["합천영상테마파크 일정에 담아줘","합천영상테마파크","일정에 담기"]]){const r=understandVoice(u);assert.equal(r.slots.place.value,p);assert.equal(r.slots.action.value,a)}});
test("unclear action preserves a confirmed place while requesting missing semantics",()=>{const original=understandVoice("미가추어탕");assert.equal(original.slots.place.confidence,"HIGH");assert.deepEqual(voiceNeedsConfirmation(original),["action","category"]);const repaired=updateVoiceSlot(updateVoiceSlot(original,"action","장소 정보 보기"),"category","장소 정보");assert.equal(repaired.slots.place.value,"미가추어탕");assert.equal(voiceExecutionText(repaired),"미가추어탕 장소 정보 보기")});
test("place-only editing preserves region action category constraints",()=>{const original=understandVoice("미가추어탕 주변 식당 찾아줘 걸어서","거창군 가조면"),repaired=updateVoiceSlot(original,"place","미가추어탕");assert.equal(repaired.slots.referenceLocation.value,original.slots.referenceLocation.value);assert.equal(repaired.slots.action.value,original.slots.action.value);assert.equal(repaired.slots.category.value,original.slots.category.value);assert.equal(repaired.slots.constraints.value,"도보")});
test("all required states have visible guidance",()=>{for(const state of["IDLE","REQUESTING_PERMISSION","LISTENING","TRANSCRIBING","UNDERSTANDING","CONFIRMING","EXECUTING","RECOVERABLE_ERROR","PERMISSION_DENIED","UNSUPPORTED"]as const)assert.ok(voiceStateMessage(state).length>5,state)});
test("high-confidence read-only requests auto execute without confirmation",()=>{for(const text of["미가추어탕 주변 식당 찾아줘","황매산 지금 추워?","해인사 몇 시까지 해?"])assert.deepEqual(voiceConfirmationPolicy(understandVoice(text)),{mode:"AUTO_EXECUTE",slots:[],reasons:[]})});
test("automatic nearby execution retains both action and requested category",()=>assert.equal(voiceExecutionText(understandVoice("미가추어탕 주변 식당 찾아줘")),"미가추어탕 주변 장소 찾기 음식점"));
test("low place confirms only place and low action confirms only action",()=>{const high=understandVoice("미가추어탕 주변 식당 찾아줘"),lowPlace=updateVoiceSlot(high,"place",""),lowAction=updateVoiceSlot(high,"action","");assert.deepEqual(voiceConfirmationPolicy(lowPlace).slots,["place"]);assert.deepEqual(voiceConfirmationPolicy(lowAction).slots,["action"])});
test("high-confidence itinerary mutation still requires final confirmation",()=>{const decision=voiceConfirmationPolicy(understandVoice("합천영상테마파크 일정에 담아줘"));assert.equal(decision.mode,"CONFIRM");assert.deepEqual(decision.slots,[]);assert.deepEqual(decision.reasons,["RISKY_ACTION"])});
test("ambiguous place and unclear region request only their affected fields",()=>{const base=understandVoice("미가추어탕 주변 식당 찾아줘");assert.deepEqual(voiceConfirmationPolicy({...base,placeAmbiguous:true}).slots,["place"]);assert.deepEqual(voiceConfirmationPolicy({...base,regionRelationship:"UNCLEAR"}).slots,["referenceLocation"])});
test("duplicate final speech results are accepted only once during execution window",()=>{const first=acceptVoiceResult(null,"미가추어탕 주변 식당 찾아줘",1000,false),duplicate=acceptVoiceResult(first.next," 미가추어탕  주변 식당 찾아줘 ",1100,true),later=acceptVoiceResult(first.next,"미가추어탕 주변 식당 찾아줘",3000,false);assert.equal(first.accepted,true);assert.equal(duplicate.accepted,false);assert.equal(later.accepted,true)});
