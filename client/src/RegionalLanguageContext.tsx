import{createContext,useContext,useEffect,useMemo,useState,type ReactNode}from'react';import{useLocation,useNavigate}from'react-router-dom';import{useRegion}from'./RegionContext';import{loadTripSession,setTripLanguage}from'./tripSession';export type RegionalLanguage='ko'|'en';const DEVICE_KEY='regional-home-language-v1',Context=createContext<any>(undefined);const valid=(x:string|null|undefined):x is RegionalLanguage=>x==='ko'||x==='en';export function RegionalLanguageProvider({children}:{children:ReactNode}){const location=useLocation(),navigate=useNavigate(),region=useRegion(),url=new URLSearchParams(location.search).get('lang'),stored=()=>{try{const trip=loadTripSession(localStorage,region.id)?.language,saved=localStorage.getItem(DEVICE_KEY);return valid(trip)?trip:valid(saved)?saved:'ko'}catch{return'ko' as const}},[language,setLanguageState]=useState<RegionalLanguage>(valid(url)?url:stored());useEffect(()=>{if(valid(url)&&url!==language)setLanguageState(url)},[url]);useEffect(()=>{document.documentElement.lang=language;try{localStorage.setItem(DEVICE_KEY,language);setTripLanguage(region.id,language)}catch{}},[language,region.id]);const withLanguage=(path:string)=>{if(language==='ko')return path;const[base,hash]=path.split('#'),[pathname,query]=base.split('?'),params=new URLSearchParams(query||'');params.set('lang','en');return `${pathname}?${params}${hash?`#${hash}`:''}`};const select=(next:RegionalLanguage)=>{setLanguageState(next);const params=new URLSearchParams(location.search);if(next==='en')params.set('lang','en');else params.delete('lang');navigate(`${location.pathname}${params.size?`?${params}`:''}${location.hash}`,{replace:true})};const value=useMemo(()=>({language,select,withLanguage}),[language,location.pathname,location.search]);return <Context.Provider value={value}>{children}</Context.Provider>}export function useRegionalLanguage(){return useContext(Context)as{language:RegionalLanguage;select:(x:RegionalLanguage)=>void;withLanguage:(x:string)=>string}}
