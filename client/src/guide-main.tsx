import{StrictMode,useState}from'react';import{createRoot}from'react-dom/client';import{askGuide,type GuideAudience,type GuideResponse}from'./guideClient';import'./guide.css';

const popular=['ChatGPT하고 뭐가 다른가요?','구글·네이버 지도가 있는데 왜 필요한가요?','업체에는 어떤 도움이 되나요?','틀린 정보는 누가 고치나요?','내 여행정보는 어떻게 관리되나요?'];
type Message={role:'user'|'guide';text:string;response?:GuideResponse};
function GuideApp(){const[input,setInput]=useState(''),[messages,setMessages]=useState<Message[]>([]),[busy,setBusy]=useState(false),[audience,setAudience]=useState<GuideAudience>('GENERAL'),last=[...messages].reverse().find(x=>x.response)?.response;const send=async(value=input)=>{const question=value.trim();if(!question||busy)return;setMessages(x=>[...x,{role:'user',text:question}]);setInput('');setBusy(true);try{const response=await askGuide(question,last?.intent,audience);setMessages(x=>[...x,{role:'guide',text:response.answer,response}])}catch(error:any){setMessages(x=>[...x,{role:'guide',text:error.message||'잠시 후 다시 시도해 주세요.'}])}finally{setBusy(false)}};return <main className="guide-shell"><header><span className="guide-kicker">CONCIERGE GUIDE COPILOT</span><h1>지역 AI 컨시어지</h1><p>무엇이 궁금하세요?</p></header>{messages.length>0&&<section className="guide-conversation" aria-live="polite">{messages.map((message,index)=><article key={index} className={`guide-message ${message.role}`}><small>{message.role==='user'?'질문':'가이드'}</small><p>{message.text}</p>{message.response?.relatedQuestions?.length?<div className="guide-related">{message.response.relatedQuestions.slice(0,2).map(q=><button key={q} onClick={()=>send(q)}>{q}</button>)}</div>:null}</article>)}</section>}<form onSubmit={event=>{event.preventDefault();void send()}} className="guide-composer"><label htmlFor="guide-question">질문을 입력하세요</label><div><textarea id="guide-question" value={input} onChange={event=>setInput(event.target.value)} rows={2} maxLength={500} placeholder="예: 구글 지도가 있는데 왜 필요한가요?"/><button disabled={busy||!input.trim()}>{busy?'답변 중…':'질문하기'}</button></div></form>{messages.length===0&&<section className="guide-popular"><h2>많이 묻는 질문</h2>{popular.map(question=><button key={question} onClick={()=>void send(question)}>{question}<span aria-hidden="true">→</span></button>)}</section>}<section className="guide-perspective"><span>다른 관점으로 보기</span><div>{([['VISITOR','관광객'],['BUSINESS','업체'],['PUBLIC_SECTOR','지자체']]as const).map(([value,label])=><button key={value} className={audience===value?'selected':''} onClick={()=>setAudience(value)}>{label} 입장</button>)}</div></section><footer><p>이 가이드는 서비스를 설명하는 읽기 전용 안내입니다.</p><small>관광 일정 실행이나 지역 운영 데이터 관리는 각 전용 서비스에서 이루어집니다.</small></footer></main>}
createRoot(document.getElementById('guide-root')!).render(<StrictMode><GuideApp/></StrictMode>);
