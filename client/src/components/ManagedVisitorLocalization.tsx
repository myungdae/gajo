import{useEffect}from'react';import{useRegionalLanguage}from'../RegionalLanguageContext';import{managedVisitorText}from'../managedVisitorCopy';
const attributes=['aria-label','title','placeholder'];
export default function ManagedVisitorLocalization(){const{language}=useRegionalLanguage();useEffect(()=>{const apply=(root:Node)=>{if(root.nodeType===Node.TEXT_NODE&&root.textContent)root.textContent=managedVisitorText(root.textContent,language);if(root instanceof Element){for(const name of attributes){const value=root.getAttribute(name);if(value)root.setAttribute(name,managedVisitorText(value,language))}for(const node of root.childNodes)apply(node)}};apply(document.body);const observer=new MutationObserver(records=>{for(const record of records){if(record.type==='characterData')apply(record.target);for(const node of record.addedNodes)apply(node)}});observer.observe(document.body,{subtree:true,childList:true,characterData:true});return()=>observer.disconnect()},[language]);return null}
