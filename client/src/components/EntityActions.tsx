import{track}from'../analytics';import{ensureTripSession}from'../tripSession';import{navigationTarget,launchNavigation,type NavigationProvider}from'../utils/placeNavigation';import{getSessionLocation,isOperationalLocation}from'../utils/visitorLocation';import{useRegion}from'../RegionContext';
type ActionSet={detail?:{url?:string};reserve?:{url?:string};call?:{phone?:string};navigate?:{latitude?:number;longitude?:number};website?:{url?:string}};
export default function EntityActions({entity,hideDetail=false,navigationLabel='내비로 가기',onNavigate}:{entity:any;hideDetail?:boolean;navigationLabel?:string;onNavigate?:(provider:NavigationProvider)=>void}){const region=useRegion(),session=ensureTripSession(region.id),actions=(entity.actions||entity.literalProps?.actions||{})as ActionSet;const id=entity.programUri||entity.uri||entity.entityUri||entity.entityId||'',name=entity.programLabel||entity.label||entity.name||'';const event=(type:any,actionType:string)=>track(type,session.id,{entityId:id,entityType:entity.entityType||entity.literalProps?.category||'UNKNOWN',actionType});const open=(url:string,type:any,actionType:string)=>{event(type,actionType);window.open(url,'_blank','noopener,noreferrer')};const navigate=(provider:NavigationProvider)=>{const latitude=Number(actions.navigate?.latitude??entity.latitude),longitude=Number(actions.navigate?.longitude??entity.longitude);if(!Number.isFinite(latitude)||!Number.isFinite(longitude))return;const location=getSessionLocation(),origin=isOperationalLocation(location)?{latitude:location!.latitude!,longitude:location!.longitude!}:undefined;onNavigate?.(provider);event('NAVIGATION_HANDOFF',provider.toUpperCase());launchNavigation(navigationTarget(provider,{name,latitude,longitude},origin),{mobile:/Android|iPhone|iPad|Mobile/i.test(navigator.userAgent)})};return <div className="entity-actions">{!hideDetail&&actions.detail?.url&&<button className="btn btn-text" onClick={()=>open(actions.detail!.url!,'ENTITY_DETAIL_OPENED','DETAIL')}>상세보기</button>}{actions.reserve?.url&&<button className="btn btn-primary" onClick={()=>open(actions.reserve!.url!,'BOOKING_HANDOFF','RESERVE')}>예약하기</button>}{actions.call?.phone&&<a className="btn btn-text" href={`tel:${actions.call.phone}`} onClick={()=>event('PHONE_HANDOFF','CALL')}>전화하기</a>}{actions.website?.url&&<button className="btn btn-text" onClick={()=>open(actions.website!.url!,'WEBSITE_HANDOFF','WEBSITE')}>홈페이지</button>}{actions.navigate&&<details><summary className="btn btn-text">{navigationLabel}</summary><div className="tag-row">{(['naver','kakao','tmap']as NavigationProvider[]).map(provider=><button key={provider} className="btn btn-text" onClick={()=>navigate(provider)}>{({naver:'네이버지도',kakao:'카카오맵',tmap:'TMAP'}as any)[provider]}</button>)}</div></details>}</div>}
